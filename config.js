// ================================================================
//  config.js — OUTBREAK ZONE 파라미터 테이블
//  수치 조정은 이 파일만 수정하면 됩니다.
//  파라미터 ID는 PARAMETER MASTER 엑셀과 동일합니다.
// ================================================================

const CONFIG = {

  // ── A. 맵 ─────────────────────────────────────────────────────
  map: {
    size:       17,   // A1  맵 크기 (홀수 유지)
    tileSize:   32,   // 타일 픽셀 크기 (고정 추천)
    loopPaths:   3,   // A7  루프 경로 수 (순환 이동 보장)
  },

  // ── B. 플레이어 ───────────────────────────────────────────────
  player: {
    moveDelay:  0.07, // B1  이동 딜레이 (초) — 낮을수록 빠름
    visionRad:   4,   // B3  시야 반경 (타일)
  },

  // ── C. 카메라 ─────────────────────────────────────────────────
  camera: {
    smooth:    0.12,  // 카메라 추적 계수 (낮을수록 부드러움)
  },

  // ── D. 미니맵 ─────────────────────────────────────────────────
  minimap: {
    size:       90,   // 미니맵 캔버스 크기 (px)
    opacity:   0.80,  // 미니맵 불투명도
  },

};
